{
  "name": "@vianoor/organization-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build && node --watch dist/main.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@vianoor/service-runtime": "0.1.0"
  }
}
