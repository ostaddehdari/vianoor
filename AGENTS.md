# قواعد توسعهٔ ویانور

- مرجع دامنه: `docs/architecture/service-catalog.json` و `docs/roadmap/requirements.json`.
- معماری میکروسرویس از ابتداست. import از یک سرویس به سرویس دیگر و دسترسی مستقیم به دیتابیس دیگر ممنوع است.
- اشتراک کد فقط برای قراردادهای فنی، زیرساخت و UI؛ Entity و قواعد کسب‌وکار در بستهٔ مشترک قرار نمی‌گیرند.
- فارسی و انگلیسی، RTL/LTR و دسترس‌پذیری هم‌زمان توسعه داده شوند؛ متن UI در فرهنگ ترجمه باشد.
- ورودی HTTP اعتبارسنجی شود. مجوز در مالک داده نیز بررسی شود؛ tenant از ورودی کاربر قابل اعتماد نیست.
- کلید واقعی، متن مشاوره و دادهٔ شخصی در Git یا log ثبت نشود.
- پیام کامیت فارسی و مربوط به تغییر مستقل باشد. شاخه‌های `stage/NN-description`؛ ادغام بدون squash برای حفظ تاریخچه.
- ادعای تکمیل فقط همراه شواهد آزمون؛ scaffold، mock و اتصال واقعی را جدا گزارش کنید.
- پیش از PR: `npm run check` و `npm run smoke`؛ تغییر قرارداد شکستن سازگاری نیازمند نسخهٔ جدید است.
- ماکاپ‌های طراحی مرجع ظاهرند؛ اعداد، تصاویر اشخاص و متن آن‌ها دادهٔ واقعی نیست.
