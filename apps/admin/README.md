# admin

Next.js application, port 3001. `npm run dev --workspace @vianoor/admin` from the root.

/fa and /en are development foundation pages only, not authenticated dashboards. No customer data or operational access exists yet. API clients, authentication and full UI arrive in stages 2–6. Design reference: docs/design/design-system.md.
