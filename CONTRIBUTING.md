# مشارکت

Node 24.19 و npm 11.9؛ از ریشه `npm ci`، سپس `npm run check` و `npm run smoke`.

هر مرحله روی `stage/NN-description`. تغییر مستقل → کامیت مستقل فارسی. مثال: «مرحله ۰۴: جلوگیری از استفاده مجدد توکن بازیابی». پس از بررسی و CI، ادغام با merge commit؛ squash تاریخچهٔ جزئی را حذف می‌کند و مطلوب نیست.

تغییر قرارداد همراه نسخه و تست سازگاری؛ تغییر دامنه همراه migration و rollback؛ تغییر UI همراه ترجمه و حالت‌های خطا. تست واقعی سرویس بیرونی جدا از mock گزارش شود. هر PR وضعیت پیاده‌سازی، آزمون و وابستگی باقی‌مانده را بنویسد.
